{
  "name": "kanban_app",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "build": "set TARGET=build&&webpack",
    "start": "set TARGET=dev&&webpack-dev-server --config webpack.development.js --devtool eval --progress --colors --hot --content-base build"
  },
  "repository": {
    "type": "git",
    "url": "kanban_app"
  },
  "author": "giles",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^0.14.0",
    "lodash": "^3.9.1",
    "node-libs-browser": "^0.5.2",
    "style-loader": "^0.12.2",
    "webpack": "^1.9.8",
    "webpack-dev-server": "^1.9.0"
  }
}
